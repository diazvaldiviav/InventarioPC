@model ProyectoInventarioASP.Models.Ups

@{
    ViewData["Title"] = "Details";
    Layout = "_Layout";
}

<h1>Detalles</h1>

<div>
     <h3 class="h3">Ups No: @Html.DisplayFor(model => model.Id)</h3>
    <hr />
    <div class="container">
        <div class="row">
            <div class="col p-2 bg-light   border">@Html.DisplayNameFor(model => model.NumInv)</div>
            <div class="col p-2 bg-light   border">@Html.DisplayFor(model => model.NumInv)</div>
            <div class="w-100"></div>
            <div class="col p-2 bg-light   border">@Html.DisplayNameFor(model => model.Marca)</div>
            <div class="col p-2 bg-light   border">@Html.DisplayFor(model => model.Marca)</div>
            <div class="w-100"></div>
            <div class="col p-2 bg-light   border">@Html.DisplayNameFor(model => model.NumSerie)</div>
            <div class="col p-2 bg-light   border">@Html.DisplayFor(model => model.NumSerie)</div>
            <div class="w-100"></div>
            <div class="col p-2 bg-light   border">@Html.DisplayNameFor(model => model.estado)</div>
            <div class="col p-2 bg-light   border">@Html.DisplayFor(model => model.estado)</div>
            <div class="w-100"></div>
            <div class="col p-2 bg-light   border">@Html.DisplayNameFor(model => model.UserName)</div>
            <div class="col p-2 bg-light   border">@Html.DisplayFor(model => model.UserName)</div>
        </div>
    </div>
</div>
<div class="container-flex p-2">
        @if (User.IsInRole("admin") || User.IsInRole("lecturaYEscritura"))
        {
            <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-orange btn-sm">Modificar</a>
            <a asp-action="Print" asp-route-id="@Model?.Id" class="btn bg-orange btn-sm">Imprimir</a>
        }
        <a asp-action="Index" class="btn bg-orange btn-sm">Volver</a>
    </div>