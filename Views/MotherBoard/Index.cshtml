@model IEnumerable<ProyectoInventarioASP.Models.MotherBoard>

@{
    ViewData["Title"] = "Index";
    Layout = "_Layout";
}

<h1>MotherBoards</h1>

@if (User.IsInRole("admin") || User.IsInRole("lecturaYEscritura"))
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
<div class="table-responsive">
<table class="table table-hover table-border" id="tabla">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumSerieId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Micro)
            </th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody id="CuerpoTablaPc">
        @foreach (var item in Model)
        {
            <tr class="filapc">
                <td>
                    @Html.DisplayFor(modelItem => item.NumSerieId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Marca)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.estado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Micro.NumSerieId)
                </td>
                @if (User.IsInRole("admin") || User.IsInRole("lecturaYEscritura"))
                {
                    <td><a asp-action="Edit" asp-route-id="@item.NumSerieId">Edit</a></td>
                    <td><a asp-action="Details" asp-route-id="@item.NumSerieId">Details</a></td>
                    <td><a asp-action="Delete" asp-route-id="@item.NumSerieId">Delete</a></td>
                }
            </tr>

        }
    </tbody>
</table>
</div>