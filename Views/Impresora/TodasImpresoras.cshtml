@model IEnumerable<Impresora>
@{
    ViewData["Title"] = "Impresora";
}

<h1>Discos Duros</h1>

<div class = "table-responsive-sm">
  <a asp-controller="Impresora" asp-action="Crear" class= "btn-info btn-sm">Crear</a>
  <a asp-controller="Impresora" asp-action="Editar" class="btn-info btn-sm">Editar</a>
  <a asp-controller="Impresora" asp-action="Borrar" class="btn-info btn-sm">Borrar</a>
  <a asp-controller="Impresora" asp-action="BuscarInv" class="btn-info btn-sm">Buscar por inventario</a>
   <a asp-controller="Impresora" asp-action="BuscarMarca" class="btn-info btn-sm">Buscar por marca</a>
<table class="table table-bordered table-info" >
  <thead>
    <tr>
      <th scope="col">Numero de inventario</th>
      <th scope="col">Numero de serie</th>
      <th scope="col">Marca</th>
      <th scope="col">Estado</th>
    </tr>
  </thead>
  <tbody>

    @foreach (var obj in Model)
    {
      <tr>
        <td>@obj.NumInvId</td>
        <td>@obj.NumSerie</td>
        <td>@obj.Marca</td>
        <td>@obj.estado</td> 
      </tr>
    }

  </tbody>
</table>
</div>
