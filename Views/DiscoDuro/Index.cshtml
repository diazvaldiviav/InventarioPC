@model IEnumerable<ProyectoInventarioASP.Models.DiscoDuro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoConexion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capacidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.motherBoard)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoConexion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.motherBoard.NumSerieId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumSerieId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumSerieId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumSerieId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
